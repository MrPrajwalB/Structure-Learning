/* AHP — Ancestral and Heuristic Partition. Grounded in §4.57 (pp.273-277):
   the order-graph view of order-based learning (Fig.173); the Ancestral Partition (AP)
   that splits variables into ordered layers A1 ≺ A2 ≺ ... ≺ Am to prune the order graph
   (Fig.174 consistent APs, Fig.175 pruning 2^8=256 → 18 states); how an AP is built from a
   data-driven supergraph G0 of surviving candidate parent sets, collapsed into strongly
   connected components (SCCs) whose topological layering forms the AP (Example 79, Fig.176);
   the Heuristic Partition (HP) into blocks B1,...,Br with pattern values PV_i(T) giving the
   admissible upper bound H_static(S) = Σ PV_i((V∖S)∩B_i) used as the A* heuristic
   (Fig.177 pattern databases, Example 80); Algorithm 71 (build HP & pattern databases) and
   Algorithm 72 (A* on the order graph using AP for successors/scoring and HP for the
   heuristic). Method due to [621]; related work He et al. [256] (A*-MM2PC), Yuan & Malone
   [763], Fan et al. [178]. */
window.ALGO_DATA = window.ALGO_DATA || {};
window.ALGO_DATA["ahp"] = {
  name: "AHP (Ancestral and Heuristic Partition)",
  oneLiner: "An exact order-based structure learner that makes optimal search scalable by partitioning the variables in two complementary ways: an <i>ancestral partition</i> groups variables into ordered layers so whole regions of the search space can be skipped, and a <i>heuristic partition</i> splits them into blocks whose precomputed best-completion scores give a tight optimistic bound — letting A* on the order graph find the globally optimal DAG while exploring far fewer states.",
  basedOnText: "AHP builds on the order-based exact-search line — dynamic programming (OptOrd) and A* both traverse the same <i>order graph</i> whose states are subsets of variables and whose paths spell out variable orderings. AHP adds two partitioning layers on top of that view: an ancestral partition (AP) that prunes which states/orderings are even allowed, and a heuristic partition (HP) that tightens A*'s admissible heuristic. Together they keep the global optimum while shrinking both the number of states expanded and the size of the search.",

  assumptions: [
    "<b>Decomposable score</b> — the network score is a sum of per-node family scores BestScore(X, U) (BIC or any decomposable score). This is what lets a path's cost accumulate along the order graph and lets per-block 'pattern values' be summed.",
    "<b>Candidate parent sets are pruned first</b> — for each variable, standard parent-set rules keep only surviving candidate parent sets. AP and HP are built from these, so their quality depends on this pruning.",
    "<b>AP must be consistent with the (unknown) true DAG</b> — each node's true parents must lie in its own or an earlier ancestral layer. When the AP is consistent the optimal ordering still exists in the pruned order graph, so no optimum is lost.",
    "<b>Admissible heuristic</b> — the HP's pattern values are optimistic upper bounds (they relax which variables are 'already available' as parents), so A* still returns the true optimum."
  ],
  input: "A dataset 𝒟 over variables V, a decomposable family-score function BestScore(X, U | 𝒟), the surviving candidate parent sets per variable, and two tunable limits: an SCC/layer size cap (used to coarsen the AP) and a block-size limit B_max (used to size the HP blocks).",
  output: "A single <b>globally optimal DAG</b> G* — the highest-scoring Bayesian network (not a local optimum, not an equivalence class) — found by A* over the order graph restricted and guided by the AP and HP.",

  idea: [
    "Order-based learning views a BN as a DAG consistent with some total order X₁ ≺ X₂ ≺ ⋯ ≺ Xₙ: each node may only take parents from earlier variables. The search space is the <b>order graph</b> — its bottom is the empty set ∅, its top is the full set V, and a step adds one variable to the set already placed (Fig.173). A path from ∅ to V is a complete ordering, and the best path is the optimal DAG. The trouble: there are 2ⁿ states, so exact search blows up.",
    "<b>Ancestral Partition (AP) — prune the search space.</b> An AP divides the variables into <i>ordered layers</i> A₁ ≺ A₂ ≺ ⋯ ≺ Aₘ. The rule is simple: a node in layer Aℓ may choose its parents only from layers A₁ ∪ ⋯ ∪ Aℓ₋₁ (and not from its own layer). This forbids many orderings — within a layer no variable can be a parent of another — so huge parts of the order graph are never built. In the paper's 8-variable example the AP {X₁,X₂} ≺ {X₃,X₄,X₅} ≺ {X₆,X₇,X₈} cuts the order graph from 2⁸ = 256 states down to just 18 (Fig.175). Crucially, if the AP is <i>consistent</i> with the true DAG (every node's real parents sit in earlier layers), the optimal path still exists in the pruned graph — so the global optimum is not lost.",
    "<b>Where the AP comes from.</b> It is read off the data, not guessed (Example 79, Fig.176): build a directed <i>supergraph</i> G₀ that connects each variable to every variable appearing in any of its surviving candidate parent sets; collapse G₀ into <b>strongly connected components (SCCs)</b>; the DAG of SCCs has a natural topological layering — those layers become the AP. If G₀ is too dense (one giant SCC), rebuild it using only the top-k candidate parent sets per variable, gradually shrinking k until every layer is below the size cap — a coarser but workable AP.",
    "<b>Heuristic Partition (HP) — tighten the bound.</b> A* ranks states by f(S) = g(S) + h(S): the score earned so far plus an optimistic estimate of the score still to come. A loose h means A* expands too many states. HP partitions the variables into <i>blocks</i> B₁ ∪̇ B₂ ∪̇ ⋯ ∪̇ B_r and, for each block, precomputes a <b>pattern value</b> PV_i(T): the best total score obtainable by finishing exactly the variables of block Bᵢ that are still unplaced, optimistically assuming every variable outside the block is already available as a parent. These pattern databases are computed once by small dynamic programs (Fig.177, Algorithm 71). At search time the heuristic is just the sum of the relevant pattern values, H_static(S) = Σᵢ PV_i((V∖S) ∩ Bᵢ) — admissible (it can only over-estimate) yet much tighter than ignoring all structure.",
    "<b>How they combine (Algorithm 72).</b> A* runs on the order graph. The <b>AP governs the moves</b>: from a state, the only allowed next variables are those in the current frontier layer, and a variable's parents may come only from earlier layers — so successors and their scores respect the ancestral order. The <b>HP supplies the heuristic</b> f(S′) = g(S′) + H_static(S′) for ranking. Because the AP never deletes the optimal ordering and the HP bound is admissible, A* still pulls the goal V off the frontier with a proven-optimal score — but it builds far fewer states (AP) and expands far fewer of them (HP) than plain A* or OptOrd. In short: <b>ancestral partition → exact per-layer/per-block solve → merge along the order.</b>"
  ],

  steps: [
    "<b>Prune candidate parent sets.</b> For every variable X, score and prune its candidate parent sets with standard rules, caching BestScore(X, U) for the survivors. Everything else is built from these.",
    "<b>Build the supergraph G₀.</b> Connect each variable X to every variable that appears in any of X's surviving candidate parent sets. This captures all possible parent–child relations left after pruning.",
    "<b>Form the Ancestral Partition (AP).</b> Collapse G₀ into strongly connected components (SCCs) and order them topologically; these ordered SCCs become the layers A₁ ≺ A₂ ≺ ⋯ ≺ Aₘ. If a layer is too big (a dense SCC), rebuild G₀ using only the top-k parent sets per variable and shrink k until every layer obeys the size cap — yielding a coarser but valid AP. (Algorithm 71, lines 1–10.)",
    "<b>Form the Heuristic Partition (HP) and pattern databases.</b> Partition the variables into blocks B₁,…,B_r (seeded from the SCC layering, then merged/split to keep each block ≤ B_max). For each block Bᵢ and each subset T ⊆ Bᵢ, compute the pattern value PV_i(T) by a small dynamic program over T, optimistically allowing parents from everywhere outside Bᵢ (and from earlier variables within T). (Algorithm 71, lines 11–14.)",
    "<b>Initialise A* on the order graph.</b> Start state S = ∅ with g(∅) = 0 and key f(∅) = H_static(∅) = Σᵢ PV_i(Bᵢ). Push ∅ onto the frontier (OPEN). The goal is the full set V. (Algorithm 72, line 1.)",
    "<b>Expand the most promising state.</b> Pop the state S with the maximal key f(S) from OPEN. If S = V, stop and reconstruct the optimal BN. (Algorithm 72, lines 2–5.)",
    "<b>Generate AP-legal successors.</b> Let the current frontier layer Aℓ be the first layer not yet fully placed. For each variable X in Aℓ that can still be added, form S′ = S ∪ {X}; X's parents are drawn only from earlier layers A₁ ∪ ⋯ ∪ Aℓ₋₁ (and earlier variables within the layer as allowed). Set g(S′) = g(S) + BestScore(X, S′ ∩ Prev). (Algorithm 72, lines 6–10.)",
    "<b>Score successors with the HP heuristic.</b> Compute H_static(S′) = Σᵢ PV_i((V∖S′) ∩ Bᵢ) and the key f(S′) = g(S′) + H_static(S′); push/update S′ in OPEN. States whose optimistic f cannot beat a known path are never expanded — that is the pruning. (Algorithm 72, lines 11–13.)",
    "<b>Repeat until the goal is reached.</b> Keep expanding best-f states. Because the AP keeps the optimal ordering in the graph and the HP bound is admissible, the first time V is popped its score is the global optimum.",
    "<b>Reconstruct G*.</b> Backtrack the recorded path from V to ∅: each step's added variable and its chosen earlier-layer parents form a family. Stitching the families together across the ancestral layers yields the globally optimal DAG G*."
  ],

  keyConcepts: [
    { term: "Order graph", def: "The search space of order-based learning: states are subsets of variables (∅ at the bottom, V at the top); a step adds one variable, allowing it parents only from those already placed. A ∅→V path is a complete variable ordering, and the best path is the optimal DAG (Fig.173)." },
    { term: "Ancestral Partition (AP)", def: "Ordered layers A₁ ≺ ⋯ ≺ Aₘ such that a node in layer Aℓ takes parents only from earlier layers. This prunes the order graph (256 → 18 states in the paper's example, Fig.175). If consistent with the true DAG, the optimum is preserved." },
    { term: "Supergraph G₀ → SCCs", def: "How the AP is obtained from data: connect each variable to every variable in its surviving candidate parent sets; the strongly connected components of G₀, ordered topologically, become the layers. Dense G₀ is rebuilt with top-k parent sets until layers fit the size cap (Fig.176)." },
    { term: "Heuristic Partition (HP) & blocks", def: "A second partition of the variables into blocks B₁,…,B_r (each ≤ B_max). Independent from the AP, it exists only to compute a tight admissible heuristic for A*." },
    { term: "Pattern value PV_i(T) / pattern database", def: "For block Bᵢ and subset T ⊆ Bᵢ, the best score to finish T while optimistically treating all variables outside Bᵢ as available parents. Precomputed once per block by small DPs (Fig.177); summed to give the heuristic." },
    { term: "Static heuristic H_static(S)", def: "The admissible upper bound A* uses: H_static(S) = Σᵢ PV_i((V∖S) ∩ Bᵢ), the sum over blocks of the pattern value of each block's still-unplaced variables. The A* key is f(S) = g(S) + H_static(S)." },
    { term: "AP for moves, HP for guidance", def: "In Algorithm 72 the AP decides which successors are legal and which parents are eligible (it shapes g), while the HP supplies H_static (it shapes the optimistic part of f). Together they preserve optimality while cutting both the number of states and the expansions." }
  ],

  animation: {
    title: "AHP on the paper's 8-variable example (Example 79, Figs.175–176): ancestral partition {X1,X2} ≺ {X3,X4,X5} ≺ {X6,X7,X8}, then exact per-layer solve, then merge along the order.",
    nodes: [
      { id: "X1", x: 0.20, y: 0.12 },
      { id: "X2", x: 0.55, y: 0.12 },
      { id: "X3", x: 0.12, y: 0.45 },
      { id: "X4", x: 0.40, y: 0.45 },
      { id: "X5", x: 0.68, y: 0.45 },
      { id: "X6", x: 0.20, y: 0.82 },
      { id: "X7", x: 0.50, y: 0.82 },
      { id: "X8", x: 0.80, y: 0.82 }
    ],
    edges: [],
    steps: [
      { caption: "<b>Goal.</b> Find the single best-scoring DAG over {X1…X8}. Exact order-based search would explore the order graph's 2⁸ = 256 states. AHP first reshapes that search with two partitions, then runs A* — keeping the global optimum but doing far less work.", ops: [{ op: "badge", text: "exact · order graph (2⁸=256 states)", kind: "info" }] },
      { caption: "<b>Step 1 — surviving candidate parent sets.</b> For each variable, prune to its best candidate parent sets and cache BestScore(X, U). Everything downstream (the AP and HP) is built from these survivors (Fig.176a).", ops: [{ op: "highlightNodes", ids: ["X1","X2","X3","X4","X5","X6","X7","X8"], cls: "hl" }, { op: "score", text: "cache BestScore(X, U) for surviving parent sets" }, { op: "badge", text: "parent-set pruning", kind: "info" }] },
      { caption: "<b>Step 2 — build supergraph G₀ and find SCCs.</b> Connect each variable to every variable in its candidate parent sets, then collapse into strongly connected components. Three SCCs emerge, giving a topological layering — this becomes the Ancestral Partition (Fig.176b–c).", ops: [{ op: "score", text: "G₀ → SCCs → topological layers" }, { op: "badge", text: "ancestral partition (AP)", kind: "info" }] },
      { caption: "<b>Layer A₁ = {X1, X2}.</b> The first (source) ancestral layer. Variables here take no parents from later layers — they sit at the top of the order. Highlight the group.", ops: [{ op: "set", name: "Layer A1", items: ["X1", "X2"] }, { op: "highlightNodes", ids: ["X1","X2"], cls: "add" }] },
      { caption: "<b>Layer A₂ = {X3, X4, X5}.</b> The second ancestral layer. Its nodes may take parents only from A₁ — never from A₃, and not from each other. This forbids many orderings, pruning the order graph from 256 states to just 18 (Fig.175).", ops: [{ op: "set", name: "Layer A2", items: ["X3", "X4", "X5"] }, { op: "highlightNodes", ids: ["X3","X4","X5"], cls: "add" }, { op: "badge", text: "order graph pruned: 256 → 18 states", kind: "good" }] },
      { caption: "<b>Layer A₃ = {X6, X7, X8}.</b> The final ancestral layer — these nodes may take parents from A₁ ∪ A₂. The AP {X1,X2} ≺ {X3,X4,X5} ≺ {X6,X7,X8} is complete. Because it is consistent with the data, the optimal ordering still lives inside the pruned graph.", ops: [{ op: "set", name: "Layer A3", items: ["X6", "X7", "X8"] }, { op: "highlightNodes", ids: ["X6","X7","X8"], cls: "add" }, { op: "badge", text: "AP complete · optimum preserved", kind: "good" }] },
      { caption: "<b>Heuristic partition (HP).</b> Independently, split the variables into blocks (here seeded from the same layers) and precompute each block's pattern values PV_i(T) — the best optimistic completion score for any subset of the block, treating outside variables as available parents (Fig.177, Alg.71). These give the admissible heuristic H_static.", ops: [{ op: "highlightNodes", ids: ["X1","X2","X3","X4","X5","X6","X7","X8"], cls: "hl" }, { op: "score", text: "precompute PV_i(T) → H_static(S) = Σ PV_i((V∖S)∩B_i)" }, { op: "badge", text: "HP pattern databases", kind: "info" }] },
      { caption: "<b>Solve A₁ exactly.</b> A* starts at ∅ and expands only AP-legal moves in the source layer. With no earlier layers, X1 and X2 take no parents — they are the roots. Place the layer's optimal sub-structure and accumulate its score.", ops: [{ op: "set", name: "Placed", items: ["X1", "X2"] }, { op: "highlightNodes", ids: ["X1","X2"], cls: "leaf" }, { op: "score", text: "g = BestScore(X1,∅) + BestScore(X2,∅)" }, { op: "badge", text: "exact solve · layer A1", kind: "good" }] },
      { caption: "<b>Solve A₂ exactly, parents from A₁ only.</b> Now expand A₂. Each node's eligible parents are restricted to {X1,X2} (and earlier within-layer where allowed). A* finds the optimal families: X3 ← X1, X4 ← X1, X4 ← X2, X5 ← X2. The HP heuristic keeps weak branches off the frontier.", ops: [{ op: "set", name: "Placed", items: ["X1", "X2", "X3", "X4", "X5"] }, { op: "highlightNodes", ids: ["X3","X4","X5"], cls: "leaf" }, { op: "addEdge", from: "X1", to: "X3" }, { op: "orient", from: "X1", to: "X3", type: "directed" }, { op: "addEdge", from: "X1", to: "X4" }, { op: "orient", from: "X1", to: "X4", type: "directed" }, { op: "addEdge", from: "X2", to: "X4" }, { op: "orient", from: "X2", to: "X4", type: "directed" }, { op: "addEdge", from: "X2", to: "X5" }, { op: "orient", from: "X2", to: "X5", type: "directed" }, { op: "score", text: "parents ⊆ A1 ; f = g + H_static (HP bound)" }, { op: "badge", text: "exact solve · layer A2", kind: "good" }] },
      { caption: "<b>Solve A₃ exactly, parents from A₁ ∪ A₂.</b> Expand the final layer. Eligible parents now span the earlier layers. A* fixes the optimal families: X6 ← X3, X7 ← X4, X8 ← X4, X8 ← X5. Each step's key f = g + H_static prunes everything that cannot beat the best path.", ops: [{ op: "set", name: "Placed", items: ["X1", "X2", "X3", "X4", "X5", "X6", "X7", "X8"] }, { op: "highlightNodes", ids: ["X6","X7","X8"], cls: "leaf" }, { op: "addEdge", from: "X3", to: "X6" }, { op: "orient", from: "X3", to: "X6", type: "directed" }, { op: "addEdge", from: "X4", to: "X7" }, { op: "orient", from: "X4", to: "X7", type: "directed" }, { op: "addEdge", from: "X4", to: "X8" }, { op: "orient", from: "X4", to: "X8", type: "directed" }, { op: "addEdge", from: "X5", to: "X8" }, { op: "orient", from: "X5", to: "X8", type: "directed" }, { op: "score", text: "parents ⊆ A1 ∪ A2 ; goal {X1…X8} reached" }, { op: "badge", text: "exact solve · layer A3", kind: "good" }] },
      { caption: "<b>Merge along the ancestral order.</b> The per-layer optimal families are stitched together respecting A₁ ≺ A₂ ≺ A₃. Because every parent points from an earlier layer to a later one, the union is automatically acyclic — a valid DAG. A* has popped the goal V, certifying optimality.", ops: [{ op: "highlightNodes", ids: ["X1","X2","X3","X4","X5","X6","X7","X8"], cls: "good" }, { op: "badge", text: "merge layers → acyclic by AP order", kind: "good" }] },
      { caption: "<b>Result — the globally optimal DAG G*.</b> Built by A* over the order graph, with the AP shrinking the state space (256 → 18) and the HP heuristic cutting expansions — yet the returned network is provably the highest-scoring one. Exact like OptOrd/A*, but far more scalable.", ops: [{ op: "highlightNodes", ids: ["X1","X2","X3","X4","X5","X6","X7","X8"], cls: "good" }, { op: "badge", text: "optimal G* returned", kind: "good" }] }
    ]
  },

  complexity: "Worst-case exponential, inheriting the order graph's 2ⁿ state space from OptOrd/A*. In practice the two partitions attack the two costs of exact search: the Ancestral Partition prunes the number of states that exist (e.g. 2⁸ = 256 → 18 in the paper's example), while the Heuristic Partition's pattern-database bound tightens A*'s heuristic so far fewer of the surviving states are ever expanded. Building the partitions adds the cost of computing the supergraph/SCCs and the per-block pattern values (DP over subsets up to size B_max), but this is paid once and keeps every layer/block small. The result preserves the global-optimum guarantee while reaching larger networks than plain A*.",
  strengths: [
    "Returns the provably globally optimal DAG — exact, no local optima — while scaling further than OptOrd or plain A*.",
    "Two complementary, independent levers: the AP shrinks the search space (fewer states), the HP tightens the heuristic (fewer expansions).",
    "Both partitions are derived from the data (supergraph → SCC layering; pattern databases over blocks) and tuned by simple size caps (layer cap, B_max), trading precomputation for search speed without sacrificing optimality."
  ],
  limitations: [
    "Still exponential in the worst case; a dense supergraph (one giant SCC) collapses the AP to a single layer, so the top-k coarsening must be relied on and the pruning weakens.",
    "Correctness of the AP's pruning rests on it being consistent with the true DAG; a poorly built AP could in principle exclude orderings (the construction keeps survivors to protect this, but it depends on the parent-set pruning).",
    "Requires a decomposable score plus the upfront cost of building the pattern databases; returns a single optimal DAG, not the whole Markov-equivalence class."
  ],
  notes: "AHP combines an ancestral partition and a heuristic partition over the order-based exact-search framework [621]. Related work sits in the same A*/order-graph family: He et al. [256] positioned A*-MM2PC by combining MMPC potential-optimal parent sets (POPS) with SCC-derived path constraints in an admissible A* search; Yuan & Malone [763] introduced the shortest-path formulation A* solves; Fan et al. [178] collapsed the parent-relation graph into SCCs, solved each SCC as an independent shortest-path sub-problem with a consistent additive pattern database, re-evaluated POPS after each leaf selection to split large SCCs and tighten h′(S) = h₁(S) + h₂(S), and proposed a lossy top-p variant provably within ε = Σᵢ δᵢ of the optimum while preserving A*'s completeness.",
  figureRefs: "Paper §4.57 (pp.273–277): Fig.173 (order-graph view); Fig.174 (two consistent ancestral partitions on the same network); Fig.175 (order-graph pruning via an AP: 2⁸=256 → 18 states); Example 79 & Fig.176 (candidate parent sets → supergraph G₀ → SCCs/topological layers = AP); Heuristic Partition with blocks and pattern values PV_i(T), H_static(S) = Σᵢ PV_i((V∖S)∩Bᵢ), F(S) = G(S) + H_static(S); Fig.177 & Example 80 (pattern databases for two blocks); Algorithm 71 (Build HP and static pattern databases); Algorithm 72 (A* on the order graph using AP for successors/scoring and HP for the heuristic)."
};
